[project]
name = "engram"
version = "0.1.1"
description = "Durable, explainable memory for AI agents — Engram tools for Dify."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.5.0",
    "requests>=2.31.0",
]
